package exrc.chpt_040

//typealias GeneratorFunc =
//typealias FilterFunc =
//typealias MapperFunc =
//typealias TerminalFunc =

fun main() {
//    val terminalFunc =
//    val mapperFunc =
//    val filterFunc =
    for (n in 1..10) {
//        val generatorFunc: GeneratorFunc =
//        process2(generatorFunc, filterFunc, mapperFunc, terminalFunc)
    }
}

//fun process2(generatorFunc: GeneratorFunc, filterFunc: FilterFunc, mapperFunc: MapperFunc, terminalFunc: TerminalFunc) {
//    val x = generatorFunc()
//    if (filterFunc(x)) {
//        val y = mapperFunc(x)
//        terminalFunc(y)
//    }
//}

