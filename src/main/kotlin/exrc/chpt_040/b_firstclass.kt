package exrc.chpt_040

//typealias SupplierFunc =
//typealias FilterFunc =
//typealias MapperFunc =
//typealias TerminalFunc =

fun main() {
//    val terminalFunc =
//    val mapperFunc =
//    val filterFunc =
    for (n in 1..10) {
//        val supplierFunc: SupplierFunc =
//        process2(supplierFunc, filterFunc, mapperFunc, terminalFunc)
    }
}

//fun process2(supplierFunc: SupplierFunc, filterFunc: FilterFunc, mapperFunc: MapperFunc, terminalFunc: TerminalFunc) {
//    val x = supplierFunc()
//    if (filterFunc(x)) {
//        val y = mapperFunc(x)
//        terminalFunc(y)
//    }
//}


